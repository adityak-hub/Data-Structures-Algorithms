CLASS--

A class is a blueprint or template used to create objects.

Example

Think of a car factory blueprint.

The blueprint says:

Color
Brand
Speed
Start()
Stop()

But you cannot drive the blueprint.

It only describes how a car should be made.

This is only the design.

No actual car exists yet.


OBJECT---


An object is a real world things that has data(attributes) and behaviour(methods/functions).
object is an instance(example) of a class.


Real Life Examples
Example 1

Class: Student

Blueprint

Name
Age
Roll No
Study()
Sleep()

Objects

Aditya
Rahul
Priya

All are students.

Each has different values.


What is a constructor?
 
A constructor is a special method that runs(call) automatically when an object is created.

•Constructor have the same as class or structure(c++).
•Constructor don't have a return type.(not even void).
•Constructor are only called once, at object creation.
•Memory allocation happens when constructor is called.

why we use constructor how it works ?

You own a car factory.

You have the blueprint:

Now you create a car.

Java creates the object.

But then Java asks:

"What should I put inside this new car?"

Should the color be red?

Should the speed be 120?

Should the engine be started?

Someone has to initialize the object.

That's exactly what a constructor does.

types of constructor-------------
1.Non-parameterized: take no argument or parameter in constructor.
2.parameterized:we take any argument or parameter.





-----------------------------------------------------------------
ACCESS MODIFIER

+-----------------+--------------+----------------+-------------------------------+------------------+
| Access Modifier | Within Class | Within Package | Outside Package (Subclass)    | Outside Package  |
+-----------------+--------------+----------------+-------------------------------+------------------+
| Private         | Yes          | No             | No                            | No               |
| Default         | Yes          | Yes            | No                            | No               |
| Protected       | Yes          | Yes            | Yes                           | No               |
| Public          | Yes          | Yes            | Yes                           | Yes              |
+-----------------+--------------+----------------+-------------------------------+------------------+


Easy Notes---
1. Private
   - Accessible only within the same class.

2. Default (No Modifier)
   - Accessible within the same class.
   - Accessible within the same package.
   - Not accessible outside the package.

3. Protected
   - Accessible within the same class.
   - Accessible within the same package.
   - Accessible outside the package only through inheritance (subclass).

4. Public
   - Accessible from anywhere.



Memory Trick
Private   -> Only the same class.

Default   -> Same class + Same package.

Protected -> Same class + Same package + Subclass (outside package).

Public    -> Everywhere.


Easy Story***************

Imagine your mobile phone.

Private*

Your OTP.

Only you can see it.

Nobody else.

Default*

Family members can use it.

Neighbours cannot.

Protected*

Family members can use.

If your son goes to another city, he can still use it because he is family (subclass).

Random strangers cannot.

Public*

Your Instagram profile.

Everyone can see it.


-----------------------
getter and setter 


Getter: A method used to read the value of a private variable.
Setter: A method used to update(set) the value of a private variable, often with validation.

----------
Encapsulation
Encapsulation is defined as the wrapping up of data and methods under a single unit.it also implements data hiding(if data is sensitive/unnecessary details(jiksi jaroorat nhi hai user ko) than use private/default/protected access specifier).
---------------------
This--

•this is a reference variable that refers to the current object.
•It is used to access the current object's variables and methods.


•If a method parameter and an instance variable have the same name, use this to refer to the instance variable. If the names are different, this is optional but often used for readability and consistency.



-----------------------
Destructor->
A destructor is a special method that is automatically called when an object is destroyed. It is used to free or clean up resources used by the object.




Does Java have a Destructor?
No. ❌

Java does not have destructors like C++.

Instead, Java uses a Garbage Collector (GC).

When an object is no longer needed, the Garbage Collector automatically frees its memory.


---------------------------------
inheritance
Inheritance is when properties and methods of base class(parent class) are passed on to a derived class(child class) it is called inheritance.

types of inheritance--

1. Single Inheritance: One child class inherits from one parent class.
Animal
   │
   ▼
 Fish

2. Multilevel Inheritance:A child class becomes the parent of another class.

Animal
   │
   ▼
 Fish
   │
   ▼
Shark

3. Hierarchical Inheritance: Multiple child classes inherit from the same parent class.
        Animal
      /    |    \
     ▼     ▼     ▼
   Fish  Bird  Dog


4. Hybrid Inheritance:Hybrid inheritance is the combination of two or more types of inheritance in the same program.
✅ A combination of single, multilevel, and hierarchical inheritance is possible in Java.
❌ Hybrid inheritance that requires multiple inheritance of classes is not supported in Java.

          Animal
         /      \
      Fish      Bird
        │          │
      Shark     Peacock



------------------------------
Polymorphism in Java
What is Polymorphism?

Definition:Polymorphism is an OOP concept where one method or object can have many forms or behaviors.

Poly = Many
Morphism = Forms

So, Polymorphism = Many Forms.

Simple Example

Think of a person.

At home → Son
At college → Student
At work → Employee

The same person has different roles (forms).

Similarly, in Java, the same method or object can behave differently.

•Types of Polymorphism
There are 2 types of polymorphism in Java.

1. Compile-Time Polymorphism (Static Polymorphism)

It is achieved using Method Overloading.

Definition
When multiple methods have the same name but different parameters(different types or count) and it is not depend on retrun type, it is called method overloading.

The compiler decides which method to call before run the code.


2. Run-Time Polymorphism (Dynamic Polymorphism)

It is achieved using Method Overriding.

Definition
When a Parent and child classes both contain the same function with a different definition(same function hai but kaam alag hai)and it doesn't depend on return type, it is called method overriding.

The method is chosen at runtime.

---------------------------------

packages in java:
package is a group of similar types of classes,interfaces and sub-packages.


-------------------
Abstraction:hiding all the unnecessary details(jo user ke kisi kaam nhi hai aur subclasses ki bhi) and showing only the important parts to the user.
In abstraction we give idea to uncoming user or subclasses how particular things work but not give implementation.

Abstract Class
•cannot create an instance(object) of abstract class
•can have abstract/non-abstract methods
•can have constructors
•abstract method woh method hote hai jinmai implementation nhi hota.
•abstract class woh class hoti hai jiske andar hum object nhi bna skte.
You cannot create an object of Animal.

❌
Animal a = new Animal(); // Error



1. What is abstraction?
Abstraction is the process of hiding implementation details and showing only the essential functionality to the user. It focuses on what an object does, not how it does it.

Example:
When you drive a car, you use the steering wheel, accelerator, and brakes. You don't need to know how the engine works internally. That's abstraction.

2. What is an abstract class?
An abstract class is a class that cannot be instantiated (you cannot create its object). It can contain both abstract methods (without a body) and concrete methods (with a body). It is used when multiple related classes share common behavior but also need their own specific implementations.

3. What is an abstract method?
An abstract method is a method declared without an implementation. Any concrete subclass that extends the abstract class must provide an implementation for that method.

Example:

abstract class Animal {
    abstract void walk();
}




-----------------------------------------
interfaces: Interface is a blueprint of a class
work of interface->
•to implements multiple inheritance(5th type of inheritance)
•to achieve total abstraction

1.All methods are public,abstract and without implementation
2.used to achieve total abstraction
3.Variables in the interface are final,public and static.


Interface vs Abstract Class
Abstract Class	                                      Interface
Uses extends	                                      Uses implements
Can have constructors	                             Cannot have constructors
Can have instance variables	                       Only constants (public static final)
Can have both abstract and normal methods	           Can have abstract methods (and also default/static methods in modern Java)
Supports single inheritance                          A class can implement multiple interfaces